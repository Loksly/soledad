<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
      LA PROMESA VERIFICABLE (docs/08 §2).

      Sin INTERNET la app es FÍSICAMENTE INCAPAZ de mostrar un anuncio, enviar telemetría o
      filtrar datos. Es lo que convierte "confía en nosotros" en un hecho que cualquiera puede
      comprobar con `aapt dump permissions`.

      El `tools:node="remove"` no es paranoia: la plantilla de Capacitor añade INTERNET por
      defecto (venía justo aquí), y cualquier librería que se añada mañana puede reintroducirlo
      en la fusión de manifiestos sin que nadie lo note. Aquí se borra pase lo que pase. La app
      se sirve desde https://localhost con el WebViewAssetLoader, que NO toca la red.

      La sincronización en la nube, si algún día se quiere, vive en el flavor `cloud`, se
      distribuye aparte y se anuncia como tal. Aquí no se cuela.
    -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />

    <!-- VIBRATE es de nivel "normal": no pide diálogo, sólo alimenta la háptica, que es
         desactivable y de la que no depende nada. Es el único permiso de la variante base. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
